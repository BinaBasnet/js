var arr = [11, 12, 22, 34, 43, 15];
arr.push(10);   // Adds 10 at the end of the array
arr.pop();      // Removes  last element
arr.shift();    // Removes  first element
arr.unshift(5); // Adds 5 to the beginning of the array

console.log(arr); // Output will be [5, 12, 22, 34, 43]
